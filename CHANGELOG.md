# 변경 이력

## 0.7.0 - 2026-08-17

- 폼 필드와 화면 UI를 의미 단위로 닫도록 안내합니다. feature와 page에서 입력 컴포넌트를 밖에서 조립하지 않습니다.
- 최소 코드보다 확장 가능한 구조를 우선하고, 한 곳에서만 쓰여도 유틸·훅·의미 단위면 분리하도록 안내합니다.
- 확인은 범위만 받고, 합의한 범위 안에서는 원칙대로 만들고 사후에 보고합니다.
- 기존 코드에 원칙을 심는 일과 가드·인가 점검을 리팩토링 흐름에 넣습니다. 인증 감사 액션도 바로 부를 수 있습니다.
- 한 줄 길이, 핸들러 이름, 폼 이벤트 타입, API 인가, RLS 기본값을 더 구체적으로 안내합니다.

## 0.6.0 - 2026-08-17

- Store와 도메인 훅, 컴포넌트의 책임 경계를 구분해서 안내합니다. 컴포넌트가 Store 값을 다른 훅에 다시 주입하거나, 훅에 성공 콜백을 주입해 UI 동작을 위탁하는 패턴을 잡아냅니다.
- 훅의 단일 책임 기준을 도메인 단위로 정리해서, 여러 상태와 effect를 가진 훅도 하나의 기능을 온전히 담당한다면 문제 삼지 않습니다.
- 기존 코드 리팩토링 점검을 컴포넌트/훅, 데이터 요청, 디자인 시스템, 컴포넌트 API, 코드 위생 5개 영역으로 나눠, 필요한 영역만 골라 점검할 수 있게 됩니다.
- mykit 자체 규칙을 추가하고 전파하는 절차를 새 액션으로 제공합니다.

## 0.5.0 - 2026-08-17

- 컴포넌트 안의 서로 다른 로직을 훅 하나에 몰아넣지 않고, 로직별로 각각 분리하도록 안내합니다.
- 같은 리소스를 다루는 API 호출을 하나의 서비스 모듈로 묶어, URL과 요청 세부사항이 호출부에 드러나지 않도록 안내합니다.
- 날짜, 숫자 포맷 같은 범용 로직은 utils/로 분리하되, 실제로 재사용이 필요해지기 전까지는 미리 추출하지 않도록 안내합니다.
- code-refactoring 액션이 리팩토링 대상을 먼저 확인하고, 규모가 큰 저장소에서는 키워드로 후보를 좁혀 탐색하며, 프론트엔드 전용 체크리스트임을 명시하도록 개선됐습니다.
- 문서 간 내용이 어긋나지 않는지 자동으로 검증하는 범위를 넓혔습니다.

## 0.4.0 - 2026-08-16

- 컴포넌트에서 fetch를 직접 호출하거나 파생 상태 로직이 몰려 있으면 훅으로 분리하도록 안내합니다.
- 요청과 실패 처리가 여러 곳에 반복되면 하나의 client 모듈로 통합하도록 안내합니다.
- 체크박스와 드롭다운처럼 디자인 일관성이 중요한 UI는 네이티브 요소 대신 프로젝트 디자인 시스템 컴포넌트를 먼저 확인하고 재사용하도록 안내합니다.
- 기존 코드의 구조적 리팩토링(컴포넌트와 훅의 경계, 데이터 요청 계층, 디자인 시스템 재사용)을 점검하는 `/mykit:code-refactoring` 액션을 추가했습니다.

## 0.3.0 - 2026-08-13

- 자주 쓰는 액션(컴포넌트 추가, 화면 구현, 코드 스타일 리뷰, 문서 업데이트, 버전 릴리즈)을 `/mykit:<이름>` 슬래시 커맨드로 실행 가능합니다.
- mykit의 안내 문구를 더 명확하고 일관되게 다듬었습니다.
- 버전 릴리즈 절차가 정리되지 않은 develop 변경, main 동기화 누락, CHANGELOG 누락 버전을 더 안전하게 처리하고, 대상 커밋을 정확히 가려내도록 개선했습니다.

## 0.2.1 - 2026-08-07

- 플랫폼 공통 카피와 tenant·사용자 런타임 데이터, fixture 문구를 구분하는 화면 작성 기준을 추가했습니다.
- 불필요한 설명과 문자 아이콘을 점검하면서 안전에 필요한 안내는 보존하는 Content QA를 추가했습니다.
- 프로젝트가 지정한 사용자 노출 경로와 금지 문자열을 검사하는 선택형 CLI를 추가했습니다.
- 영문·한글 템플릿과 생성 플레이북에 같은 카피·아이콘·화면 완료 계약을 반영했습니다.

## 0.2.0 - 2026-08-07

- 승인된 디자인과 반응형 요구사항이 primitive 재사용보다 우선하도록 UI 책임 경계를 명확히 했습니다.
- 새 화면 구현을 위한 build-screen 절차와 구조적·시각적 검증 기준을 추가했습니다.
- 내부 CSS Module 적용과 public className/style 탈출구를 구분하고 화면별 layout CSS 소유권을 명시했습니다.
- 영문·한글 템플릿과 생성 플레이북의 핵심 UI 계약이 어긋나지 않도록 자동 검사를 추가했습니다.

## 0.1.3 - 2026-08-07

- 컴포넌트 추가/릴리즈/코드 스타일 리뷰/문서 업데이트 네 가지만 남기고 나머지 액션 플레이북과 EXAMPLES.md 정리.
- 컴포넌트 추가 작업 전 checklist.md/context-notes.md 산출물 요구 절차 제거.
- 컴포넌트 계층 철학 문서에 layout prop 계약, className/style escape hatch 예시, size/color 토큰 강제, 계층 건너뛰기 금지 규칙, 확장/사용/조합/구체화 관계 어휘, variant named subcomponent 권장 규칙 추가.

## 0.1.2 - 2026-07-23

- develop→main 릴리즈 절차를 표준화하는 버전 릴리즈(release-version) 액션 플레이북 추가.
- main/develop 동기화가 누락된 경우 먼저 확인받는 절차 추가.
- 사용자의 명시적 승인 없이는 커밋하지 않도록 커밋 정책 강화.

## 0.1.1 - 2026-07-21

- Claude Code 스킬 패키지 이름을 `mykit`으로 변경.
- 컴포넌트, API 엔드포인트, 페이지, 데이터 모델, 테스트, 리팩터링, 버그 수정, 성능 개선, 보안 검토, 문서, 기능 오케스트레이션, 라이브러리 연동, 백그라운드 작업, PR 리뷰용 재사용 가능한 액션 플레이북 추가.
- 장기적으로 유지되는 엔지니어링 철학을 `skills/mykit/references/philosophy/`로 분리.
- 생성되는 Claude 가이드 템플릿을 단순화하고 기존 Cursor rule 출력 제거.
- README에 GitHub 사용법 안내 추가.
- MIT `LICENSE` 파일을 추가하고 패키지/플러그인 메타데이터를 MIT 기준으로 정렬.
- npm 패키지에 포함되는 파일을 CLI 소스, 템플릿, mykit skill, 플러그인 메타데이터, README, 라이선스 파일로 제한.
- `node_modules/`, `package/`, 오래된 `*.tgz` 아카이브처럼 이전에 추적되던 로컬 산출물을 Git 인덱스에서 제거.

## 0.1.0 - 2026-05-06

- 최초 `mykit init` CLI 추가.
- 인터랙티브 및 플래그 기반 스택 선택 기능 추가.
- 영어/한국어 플레이북 템플릿 추가.
- 생성되는 `PLAYBOOK.md`, `playbook/PLAYBOOK.index.yaml`, 스택 문서, AI 가이드 파일 추가.
